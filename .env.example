# GuardNG client configuration
# In dev you can leave this unset — Vite proxies /api to localhost:4000.
# In production set it to your deployed API origin, e.g. https://api.guardng.app/api
VITE_API_URL=
